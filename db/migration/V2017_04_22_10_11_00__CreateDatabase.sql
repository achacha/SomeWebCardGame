-- Since flyway needs this database to add versioning into, it auto-creates the database

-- -----------------------------------------------------
-- Create database and assign owner
--
-- DROP DATABASE necropolis;

-- CREATE DATABASE necropolis
--   WITH OWNER = sawcog
--   ENCODING = 'UTF8';
--   TABLESPACE = pg_default
--   LC_COLLATE = 'en_US.UTF-8'
--   LC_CTYPE = 'en_US.UTF-8'
--   CONNECTION LIMIT = -1;
